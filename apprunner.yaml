version: 1.0
runtime: python3 
build:
  commands:
    build:
      - pip install pipenv
      - pipenv install 
      - pip install -r requirements.txt
run: 
  runtime-version: 3.6
  command: python app.py
  network: 
    port: 80
    env: MY_APP_PORT
